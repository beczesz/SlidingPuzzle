<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.exarlabs.android.slidingpuzzle"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".SlidingPuzzleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">


        <activity
            android:name=".ui.splash.SplashScreenActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <activity
            android:name=".ui.SlidingPuzzleActivity"
            android:screenOrientation="portrait">

        </activity>


        <!-- Special activity required by the Layoutcast -->
        <activity android:name="com.github.mmin18.layoutcast.ResetActivity"/>
    </application>

</manifest>
